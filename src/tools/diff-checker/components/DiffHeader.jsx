import { FileText } from "lucide-react";

const DiffHeader = () => (
  <div className="flex flex-col items-center gap-3 mb-6 pt-8">
  
    <h1 className="heading mb-1 text-center animate-fade-up">Diff Checker </h1>
    <p className="description  text-center animate-fade-up">Compare two files and highlight the differences</p>
  </div>
);

export default DiffHeader;
